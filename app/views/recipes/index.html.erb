<div id="recipes">
  <% @recipes.each do |recipe| %>
    <% link_to recipe_path(recipe.id) do %>
    <%= render recipe %>
    <% end %>
  <% end %>
</div>

<%= link_to "New recipe", new_recipe_path %>
