<%= form_with(model: RecipeFood.new, url: recipe_foods_path, local: true) do |form| %>
<% if @recipe_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@recipe_food.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
        <% @recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div>
  <%= form.label :Ingredient %>
  <%= form.collection_select(:food_id, @foods, :id, :name, { prompt: "Select a Food" }, { class: "form-select" }) %>
</div>


</div>

<div>
  <%= form.label :Quantity %>
  <%= form.text_field :quantity %>
</div>

<div>
  <%= form.submit %>
</div>
<% end %>
